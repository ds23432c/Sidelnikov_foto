[build]
builder = "NIXPACKS"

[deploy]
preDeployCommand = "python manage.py migrate --noinput && python manage.py init_data && python manage.py ensure_superuser"
startCommand = "python manage.py collectstatic --noinput && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
